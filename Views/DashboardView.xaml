<UserControl x:Class="LibraryManagementSystem.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:LibraryManagementSystem.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewmodels:DashboardViewModel />
    </UserControl.DataContext>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <materialDesign:Card Grid.Column="0" Margin="8" Padding="16">
            <StackPanel>
                <TextBlock Text="Total Books" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding TotalBooks}" FontSize="28" FontWeight="Bold"/>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Column="1" Margin="8" Padding="16">
            <StackPanel>
                <TextBlock Text="Total Patrons" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding TotalPatrons}" FontSize="28" FontWeight="Bold"/>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Column="2" Margin="8" Padding="16">
            <StackPanel>
                <TextBlock Text="Active Checkouts" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ActiveCheckouts}" FontSize="28" FontWeight="Bold"/>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Column="3" Margin="8" Padding="16">
            <StackPanel>
                <TextBlock Text="Overdue Books" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding OverdueBooks}" FontSize="28" FontWeight="Bold"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>