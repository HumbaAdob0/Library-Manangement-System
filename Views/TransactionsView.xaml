<UserControl x:Class="LibraryManagementSystem.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:LibraryManagementSystem.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <!-- DataContext will be provided from code-behind via DI -->

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search and actions -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBox Width="320"
                     Margin="0,0,8,0"
                     materialDesign:HintAssist.Hint="Search by patron or book"
                     Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Search" Command="{Binding SearchCommand}" Style="{DynamicResource MaterialDesignRaisedButton}" Margin="0,0,8,0"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{DynamicResource MaterialDesignOutlinedButton}" Margin="0,0,8,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Checkout" Command="{Binding CheckoutCommand}" Style="{DynamicResource MaterialDesignFloatingActionMiniAccentButton}" Margin="8,0,0,0"/>
                <Button Content="Return" Command="{Binding ReturnCommand}" Style="{DynamicResource MaterialDesignRaisedButton}" Margin="8,0,0,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Transactions}" SelectedItem="{Binding SelectedTransaction, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="False"
                  CanUserAddRows="False"
                  RowHeaderWidth="0"
                  SelectionMode="Single"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Patron" Binding="{Binding Patron.FullName}" Width="2*"/>
                <DataGridTextColumn Header="Book Title" Binding="{Binding Book.Title}" Width="2*"/>
                <DataGridTextColumn Header="Checkout Date" Binding="{Binding CheckoutDate, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="Return Date" Binding="{Binding ReturnDate, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer / notes -->
        <TextBlock Grid.Row="2" Text="Select a transaction and click Return to process returns." Foreground="Gray"/>
    </Grid>
</UserControl>